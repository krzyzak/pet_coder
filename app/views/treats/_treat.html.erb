<%= content_tag(:div, id: dom_id(treat), class: "absolute w-20 h-20 p-1", style: "left: #{treat.position.x * 80}px; top: #{treat.position.y * 80}px") do %>
  <%= render partial: "treats/treat_image", locals: { treat: item } %>
<% end %>
