<%= content_tag(:div, class: "absolute w-20 h-20 p-1", style: "left: #{treat.position.x * 80}px; top: #{treat.position.y * 80}px") do %>
  <%= image_tag "media/treats/#{treat.image_name}", class: "w-full h-full object-contain" %>
<% end %>
