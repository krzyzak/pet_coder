<% @turbo_actions.each do |action, params = nil| %>
  <% if params %>
    <%= turbo_stream.public_send(action, params) %>
  <% else %>
    <%= turbo_stream.public_send(action) %>
  <% end %>
<% end %>
